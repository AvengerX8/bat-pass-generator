import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    color: 'grey',
    fontSize: 35,
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 10
  },
  img: {
    height: 250,
    resizeMode: 'contain',
    borderWidth: 3
  }
});